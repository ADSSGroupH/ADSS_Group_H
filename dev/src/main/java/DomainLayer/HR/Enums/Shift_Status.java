package DomainLayer.HR.Enums;

public enum Shift_Status {
    SUCCESS,
    SHIFT_MANAGER_REMOVED,
    EMPLOYEE_NOT_FOUND,
    SHIFT_NOT_FOUND,
    INVALID_FIELD,
    INVALID_VALUE_TYPE,
    INVALID_LIST_TYPE
}
